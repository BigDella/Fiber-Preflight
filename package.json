{
  "name": "fiber-preflight",
  "version": "0.1.0",
  "private": true,
  "description": "Payment route confidence scoring and failure diagnostics infrastructure for Fiber Network (Nervos CKB)",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @fiber-preflight/web dev",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.28.2"
}
